/** Bundle root; runtime contributions are mounted through subpath rows. */
export const name = 'dsh-antfarm-bundle'

/** Root bundle entry contributes no runtime behavior. */
export function apply(): void {}
